[package]
name = "custody-chain"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "26.1.0"
pharma-types = { path = "../pharma-types" }

[dev-dependencies]
soroban-sdk = { version = "26.1.0", features = ["testutils"] }
batch-registry = { path = "../batch-registry" }
